---
to: <%= dir %>/reducers/<%= name %>.js
---
import { handleActions } from 'redux-actions';
import { } from '../actions';

const defaultState = {};

const reducerMap = {

};

export default combineActionReducers(reducerMap, defaultState);
