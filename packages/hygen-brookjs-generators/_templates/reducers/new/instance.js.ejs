---
to: <%= dir %>/reducers/<%= name %>.js
---
import { combineActionReducers } from 'brookjs';
import { } from '../actions';

const defaults = {};

const cond = [

];

export default combineActionReducers(cond, defaults);
