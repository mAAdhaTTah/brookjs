---
to: <%= dir %>/components/<%= name %>.js
---
import { component, children, render } from 'brookjs';
import template from './<%= name %>.hbs';

export default component({
    children: children({

    }),
    events: events({

    }),
    render: render(template)
})
