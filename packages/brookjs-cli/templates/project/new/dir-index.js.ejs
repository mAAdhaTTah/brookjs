---
to: "<%= name %>/<%= dir %>/index.<%= locals.typescript ? 'tsx' : 'js' %>"
---
import '@babel/polyfill';
import React from 'react';
import { createStore } from 'brookjs';
import { RootJunction } from 'brookjs-silt';
import { connect, Provider } from 'react-redux';
import ReactDOM from 'react-dom';
import { init } from './actions';
import { App } from './components';
import { rootDelta } from './deltas';
import { reducer, mapStateToProps } from './state';

const { __INITIAL_STATE__ = {} } = global;
const store = createStore(reducer, __INITIAL_STATE__, rootDelta);
const Root = connect(mapStateToProps)(App);

ReactDOM.render(
  <Provider store={store}>
    <RootJunction root$={root$ => root$.observe(store.dispatch)}>
      <Root />
    </RootJunction>
  </Provider>,
  document.getElementById('app')
);

store.dispatch(init());
