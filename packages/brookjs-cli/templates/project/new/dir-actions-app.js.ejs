---
to: "<%= name %>/<%= dir %>/actions/app.<%= locals.typescript ? 'ts' : 'js' %>"
---
import { createAction } from '<%= locals.typescript ? 'typesafe-actions' : 'redux-actions' %>';

export const init = createAction('INIT');

export const incrementClick = createAction('INCREMENT_CLICK');

export const decrementClick = createAction('DECREMENT_CLICK');
