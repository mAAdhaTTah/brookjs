<div {{container "parent"}}>
    <h1>Attach/Detach Template</h1>

    {{#each children}}
        <div {{#if id}}{{container "child"}} {{key id}}{{/if}}
                {{animate "animated"}}>
            Attached: {{text}}
        </div>
    {{/each}}
</div>
