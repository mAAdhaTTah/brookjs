sudo: required
language: node_js

node_js:
  - stable

before_install:
  - yarn global add greenkeeper-lockfile@2

before_script:
  - npx lerna bootstrap
  - npx lerna run build
  - greenkeeper-lockfile-update

script:
  - yarn test

after_script:
  - greenkeeper-lockfile-upload
